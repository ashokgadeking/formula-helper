# State files (local to each device — root-level only)
/mix_log.json
/settings.json
/countdown_state.json
/backup_status.json
/weekly_ntfy_state.json

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Images (large binary assets, except web icons)
*.png
!web/*.png

# IDE
.vscode/
.idea/

# AWS SAM
.aws-sam/
samconfig.toml

# Python venv
.venv/

# Build artifacts
lambda_backup.zip
state_cache.json
ios/build/

# Baby files (user content)
baby_files/
exports/

# App Store Connect API keys — must never be committed
*.p8

# Claude Code per-user settings
.claude/settings.local.json
